import 'package:flutter/material.dart';

class HesabimEkrani extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Hesabım ekranı içeriği buraya gelecek'),
    );
  }
}