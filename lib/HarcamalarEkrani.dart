import 'package:flutter/material.dart';

class HarcamalarEkrani extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Harcamalar ekranı içeriği buraya gelecek'),
    );
  }
}