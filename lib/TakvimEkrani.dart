import 'package:flutter/material.dart';

class TakvimEkrani extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Takvim ekranı içeriği buraya gelecek'),
    );
  }
}